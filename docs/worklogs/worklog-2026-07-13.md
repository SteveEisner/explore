# Worklog — 2026-07-13

Dated log of milestones. Newest entries at the top. See AGENTS.md for conventions.

## 2026-07-13 08:46 — The 47s turn decomposed: cache-miss economics + backend session rotated (main session)

Steve's 30+ second tool request diagnosed from the event log, in two layers. **The turn itself** (47.6s ask_artifact_agent): 64% was the model typing one huge tool call — a full-artifact rewrite as a single vault line_replace, ~3K output tokens streaming continuously — and the rest was per-turn context weight. **The economics underneath:** the 5-day log totals $335.98, of which $161 is 63 full cache rebuilds and **$125.79 went to 52 pre-warm turns that produced ≤10 output tokens** while re-caching the whole 150-400K-token transcript. Cause chain: the appended system prompt embeds the live D7 wiki preload and is rebuilt per spawn; any respawn (tsx-watch server restarts, model-switch delegations) changes the prompt prefix ~8K tokens in; the prefix-keyed cache invalidates; the connect-time pre-warm then pays full re-cache on every browser reload. Even clean hits pay ~5-6s/call restoring the ~360K context (fresh eval sessions: TTFT 1.4s at 6K cached vs 4.9s at 1.07M). Fix directions banked in TASKS (stable prefix, conditional pre-warm, session rotation).

First mitigation applied: the immortal backend session (57f3d8f2, since 07-08 01:26, 127 turns) was retired — session file moved to claude-session.json.retired-2026-07-13, server restarted, and the pre-warm confirmed spawning a fresh session (7196c343, no --resume). Next turns start from a ~25K-token context.
