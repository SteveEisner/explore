# Worklog — 2026-07-12

Dated log of milestones. Newest entries at the top. See AGENTS.md for conventions.

## 2026-07-12 22:34 — Screenshots (and pointing) follow the expanded artifact (main session)

The banked capture gap, fixed on Steve's report: with an artifact maximized, every capture path photographed the hidden document underneath. App now picks the *active surface* at call time — the expanded overlay's scroller/content when one is up (refs exposed by expanded-artifact.tsx), the document wrapper otherwise — and routes the chat screenshot button, voice take_screenshot, the state tool's capture AND its scroll/selection snapshot, and the indicate provider through it, so agents point at the artifact the user is actually looking at too. Annotations drawn on the expanded surface ride along (the pen layer lives inside the captured content wrapper). Verified through the real provider: with the overlay scrolled to 249px and the doc at 0, the snapshot reports 249 while expanded and 0 after minimize.

## 2026-07-12 22:20 — Seven-hour review written, dashboard inline per D8 (The Optimizer)

docs/reports/seven-hour-review.md: block-5 accounting (gross ~8h07 / chargeable ~7h45 / ~15 min left), verdict (feature collection → one product story; D8 decision-to-shipped in one evening; J1 grading now four reviews unpaid), journeys, process notes, and a spend-nothing-new plan for the final 15 minutes. The dashboard is an inline ```oui block in the review itself — the first report authored in the D8 form it describes.

## 2026-07-12 22:08 — Worker 2 signing off

Steve called it a day. All Worker 2 work is committed (latest: `ae62a79`, the frontend-design skill + raw-HTML doctrine); the remaining working-tree changes are the main session's in-flight D8 inline-artifact implementation. This session's arc for the record: the realtime voice agent went from spec to working end-to-end (rows 1–8 + live validation; root-cause of the silent mic was Chrome's stale audio service), then matured through the day — extreme-brevity persona per the Realtime Prompting Guide, chat collapsed into a toolbar on-air pill (green, level-reactive, latest-action ticker) with the expander-only conversation pane, voice auto-start/stop tied to chat open/close, the keyboard-typing loop over bridged tool calls with the hands-on-the-computer persona, and Anthropic's frontend-design skill wired into both agents. Open threads live in TASKS.md (voice row 9 eval with The Optimizer; row 12 delegation streaming progress).

## 2026-07-12 22:07 — D8 shipped: inline ```oui blocks render, maximize, and every .oui migrated in (main session)

The two banked D8 renderer rows are done. A fenced ```oui block on a wiki page now renders as the same launchable preview an embed gets (chat messages keep plain code blocks — only pages with a sourceUrl intercept the fence). Maximize accepts inline blocks: `app/expanded-artifact` takes {doc, line} — the page URL plus the fence's opening line — and the expanded view re-reads the block from the page, so it hot-reloads with ordinary wiki edits; the voice `expand_artifact` tool accepts .md pages (first block), and both set-state tool descriptions document the new form. The preview card also got Steve's polish: a thin raised surface (hairline ring + contact/drop shadow) and a cursor-following radial shine on the shader, so it reads as glass over a launchable app rather than an inline image.

Migration per Steve: every standalone .oui in the wiki (launch-demo, wiki-deck, design/explainer, reports/six-hour-review, examples/pr-502764-review — 76KB benchmark included) is now an inline block at the top of its sibling .md (created wiki-deck.md; replaced the embed tags elsewhere; oui-embed-demo.md rewritten around the inline form with a live example, keeping `<oui-embed src>` documented for cross-page reuse), and the .oui files are deleted — zero remain. All five pages verified rendering in the browser: one preview each, no raw fences, no error cards; expand/minimize round trip checked on launch-demo ({doc, line:3}, pill "launch-demo.md artifact").

## 2026-07-12 22:01 — Break (The Optimizer)

The cohesion hour is done; Steve is letting the workers quiet down, then taking a break before the wrap-up and write-up. The evening put ~35 commits across the fleet into functionality + polish: the D8 inline-artifact model (instructions live, renderer/maximize implementation in flight in oui-embed.tsx as of this writing), voice persona matured (hands-on-the-computer, document-first incremental builds, user-cannot-edit), smart delegation at 3.5s with no respawn, recursive wiki preload, demo scope narrowed (publishing P2, manual editing P2), and the wiki home page rewritten as the front door. Time: tonight's attended work ≈ 1h25 (docs reorg ~19:45–20:00 + the hour ~20:55–22:01), putting chargeable at ≈ 7h45 of the 8-hour allotment — **~15 minutes of build time remain**; the wrap-up write-up itself is outward reporting, excluded per precedent. Resume step: take stock once the fleet is quiet, then the final review (the "eight-hour review", by the established naming) + its .oui update.

## 2026-07-12 21:59 — Anthropic's frontend-design skill wired into both agents; raw HTML declared the design medium (Worker 2)

Fetched the canonical **frontend-design** skill (anthropics/claude-code frontend-design plugin — the "avoid AI slop" design doctrine: subject-grounded palette/typography, plan-then-critique process, one aesthetic risk, restraint) and installed it as `server/skills/frontend-design/` with an "In this app" adaptation section (style-block token system, hook classes, `.stack` scoping, system font stacks, screenshot self-critique via the state tool). The Claude session materializes it at spawn like explaining-clarity; a new "## Design" section in system-prompt.md makes loading it before any artifact creation/restyle deterministic. The shared oui-lang.md now states plainly that **raw HTML inside Content is a first-class design medium** — tables, SVG, gradients, keyframe animation — and components are layout scaffolding + editing points, not the look. The voice agent gets a three-bullet distillation (subject-grounded tokens, no stock AI looks, pass design direction into power-tool requests). Prompt build smoke-tested; 8/8 intelligence tests green.

## 2026-07-12 21:49 — Voice agent taught the new-artifact method: document first, fill incrementally, narrate between steps (The Optimizer, per Steve)

voice-agent.md gains "Creating a new artifact": create the markdown file immediately and open it, fill it in across multiple tool calls as the normal mode (skeleton, then one region at a time), and give one-clause updates between steps — the doc grows live on screen while the agent narrates phone-style.

## 2026-07-12 21:57 — Pen draws on expanded artifacts (main session)

The drawing overlay lived only inside the document wrapper, so pen strokes on an expanded artifact landed on the hidden page beneath. The expanded surface now carries its own DrawingOverlay inside its scrolled content (strokes in the artifact's coordinates, glued through scrolling) with its own stroke state — same eraser semantics (pen off clears; closing drops them). Verified via pointer-event simulation: stroke renders in the annotation layer, clears on pen-off. Nit banked: while an artifact is expanded, the screenshot paths (chat button, take_screenshot, state tool) still capture the hidden document underneath.

## 2026-07-12 21:45 — Voice tools sound like typing; persona owns the work (Worker 2)

Steve's keyboard-loop recording (moved to `client/public/sounds/tool-call-loop.mp3`, 22s seamless loop) now plays at low volume for exactly the life of every *bridged* tool call — started when the browser forwards a voice:tool to the back end, stopped when the result returns (overlap-counted so concurrent calls share one loop; hard-stopped on session end). Front-end tools (state reads, indicate) stay silent — they're instant. The persona doctrine shifted to match: the voice agent's tools ARE it using the computer — `ask_artifact_agent` reframed from "hand a job to the engine" to "your power tool"; the guidance ("One collaborator, hands on the computer") and the tool description now both forbid presenting any call as consulting/handing off, and tell the model the audible typing is its own hands.

## 2026-07-12 21:45 — Blitz round five: the bank cleared, including the two rows that appeared mid-round (the Cleaner)

Steve said "fix the rest of the bank"; seven items closed across five agents plus one inline fix:

- **oui-embed origin check** (inline) — `src.includes("://")` missed protocol-relative `//host/path`; sources now resolve via `new URL(src, origin + "/docs/")` and must land on our origin. All cases verified (bare, subdir, absolute, protocol-relative, full URL).
- **Wiki rename/move** — `renameDoc` in wiki-service (same-extension required, no-overwrite atomically via link+unlink, target dirs created), exposed as voice `rename_doc` + wiki-MCP `rename_file`, CLI-allowlisted; watcher verified to fire wiki:changed for both paths. 90/90 tests.
- **createDoc polish** — empty content rejected once in the service (schemas mirror), exists-check race replaced by `writeFile wx`, errors surface-neutral.
- **Vault meta relocation** — definitively not configurable in markdown-vault-mcp 2.3.0 (whole env surface read at dist/index.js:109–121; `meta/` hard-coded in five files). Solved with a tracked symlink `docs/meta -> design/api` + gitignored generated files + tracked `.gitkeep` for the clean-clone dangling case; verified against the real vault (no re-creation, hashes unchanged). Side effect: the meta files now index under `design/api/`.
- **Screenshot-per-voice-turn: closed against.** Un-orderable with server-VAD auto-response (capture must beat the VAD commit; short deictic utterances lose the race), resident images ≈765 tokens/turn nearly fill the 32K Realtime context by turn 30 (~30–100× cost), and speech-start capture is *staler* than on-demand `take_screenshot`. If deixis quality ever falls short, the lever is server-side `create_response: false`, not per-turn attachment.
- **.oui validated at write time** — wiki-service parses .oui creates and raw-text edits with lang-core (create refused before the file exists; a parse-breaking edit leaves the file byte-identical) with surface-neutral teaching errors. `edit_artifact` proven unable to corrupt (mergeStatements drops garbage) — but it silently no-ops on garbage, banked.
- **Broken artifacts say why** — GenerativeView forwards the Renderer's onError; a destructive-tinted card with the parser message replaces the empty sliver in both the embed preview and the full-page .oui view, clearing automatically when a hot-reload fixes the file. Verified live against the broken explainer.oui.

Bank now holds three follow-ups: the delete trash-layer question, the edit_artifact silent no-op, and regenerating explainer.oui.

## 2026-07-12 21:44 — Demo scope declared: publishing P2, manual editing P2 (The Optimizer, per Steve)

Two scope calls recorded in TASKS.md's journeys section: publishing/saving artifacts beyond the live authoring session is P2 (live in-session use is the demonstration), and the user never edits anything manually — all edits go through the front-end agent. voice-agent.md gains "The user cannot edit — you do": never explain how to make a change; ask what they want, never that they do it.

## 2026-07-12 21:50 — Delete tool, shared OpenUI explainer for both agents, launch chrome polish (main session)

Four asks landed in one thread. (1) **delete_doc / delete_file** in all the standard places — shared `deleteDoc` in wiki-service (permanent, ENOENT teaches), voice server tool + wiki-MCP tool + CLI allowlist + wiki.md guidance; descriptions demand explicit user intent per call (this was Steve's direct request — the "confirm delete decision" backlog row is resolved and reduced to the trash-layer question). Rode into git via the rename agent's commit. (2) **Both agents now learn the same OpenUI Lang**: the Syntax+Components teaching moved out of system-prompt.md into `prompts/oui-lang.md` (with the {{COMPONENT_SIGNATURES}} substitution), injected into the Claude prompt via {{OUI_LANG}} and appended verbatim to the voice session instructions at mint — the design/explainer.oui hallucination can't recur from an unguided voice model. (3) **PR explainer page**: `examples/pr-502764-review/pr-502764-review.md` created via the agent's create_doc, embedding the benchmark artifact as a launchable preview next to its source. (4) **Launch chrome per Steve's redesign**: the expanded artifact no longer has its own title bar — it shows as a closable pill riding the right side of the toolbar's file bar (doc name stays put, saying the page is still open); expand/collapse got a 300ms fade+rise+scale animation (enter via forced reflow so it works without rAF; exit has transitionend + timeout backstop, overlay stays mounted through the fade). Expand/minimize are also agent tools now: voice `expand_artifact`/`minimize_artifact`, the documented store key for the Claude session, and `expandedArtifact` is a first-class field in the state snapshot both agents read. All 90 tests green.

## 2026-07-12 21:30 — Clarity pass over both codebases (Cleaner)

Full read of server/src (16 files) and client/src (~30 app files) against the code-clarity / comment-clarity skills. Verdict: both sides already follow the standard closely; findings were drift, not style. Fixed on sight (per Steve, overriding the bank-don't-fix default): three comment/code drifts from the wiki-MCP expansion — claude.ts described the wiki server as "file-listing" (now list/read/create, two spots) and the wiki-service header still claimed "non-MCP consumers only" / "voice bridge forwards errors" (now three consumers, all forwarding verbatim) — plus an intent comment on ui-mcp's line-count acknowledgement (flavor text, not parsing) and the client ChatStatusEvent union missing "warming"/"ready" (server broadcasts them, statusText renders them, the "mirror of server protocol" claim now holds). Edits rode into concurrent commits (8632bab, 6b82ae1). Banked two nits in the blitz backlog: createDoc polish (surface-mismatched teaching errors, empty-content boundary in the wrong layer, existsSync-then-write race → `wx` flag) and oui-embed's same-origin check passing protocol-relative `//host` URLs. Deliberately untouched: the expanded-artifact cluster (in flight at review time) and the shadcn-registry ui/ primitives (coherent vendored style).

## 2026-07-12 21:28 — D8: artifacts live inline in markdown; screenshot-first observation (The Optimizer)

Two instruction sets defined with Steve and recorded. D8 (decisions.md): the canonical artifact is an OpenUI block inline in a markdown doc — start from an empty .md, the block renders in place, the user maximizes to work with it; carrier is a fenced ```oui code block (an HTML-tag carrier would let the parser eat the spec's literal HTML); separate .oui files demoted to reuse/legacy. Both prompt docs teach the construction story, and both gain a see-means-look rule: when the user references the screen ("this", "look at", "can you see"), observe via screenshot as the PRIMARY method with the state snapshot for orientation — backend `state {screenshot: true}`, voice `take_screenshot` + `get_app_state`. Two renderer rows banked (fenced-oui rendering; maximize accepting inline blocks) — until they land, inline blocks degrade to visible code.

## 2026-07-12 21:27 — Blitz round four: eight agents, seven backlog rows cleared (the Cleaner)

Steve called the blitz; eight agents ran in parallel, each item its own gated commit:

- **Vite dev proxy follows PORT** — /ws, /docs, /api targets read `process.env.PORT`.
- **Home page shares the reading-column gutters** — `READING_COLUMN_CLASS` extracted to lib/layout.ts, used by home-view and file-viewer.
- **Voice guidance: deixis + brevity** — "this/that/it/here" resolve against the current view via get_app_state before clarifying questions (screenshots only when visual detail matters, never every turn); quick local actions run unannounced, phone-style narration stays for delegated work only.
- **Incremental artifact creation** — guidance already written by a concurrent agent; our agent verified it against the ui tool's real merge semantics (small first call with `name`, then edit patches) and it landed separately.
- **Chat pane stays pinned while streaming** — root cause was the scroller's wheel escape hatch: any inertia tick at the bottom flipped to free-scrolling with no scroll event to re-pin. Wrapper-level onWheel re-pins on downward/neutral wheel at the bottom; scrolled-up readers are never yanked. Live-verified four cases.
- **Indicate emphasis registers now** — 1.5s of pulses → one 5s pass: double-pulse onset, ~1.8s hold, 2s fade.
- **"Invalid artifact filename" diagnosed** — likely culprit: the model's suggested title (e.g. with a colon) prefilled verbatim and failing the ASCII rule with a generic error. Errors now name the offending character/rule; generated defaults are sanitized to saveable before prefill; charset deliberately kept in lockstep with wikiDocPath (they must loosen together).
- **Voice screenshot fits the data channel** — capture downscaled/re-encoded (1024px edge, q0.6, shrink passes against a 200K-char budget): worst-case 1.8MB → 95KB, measured. Delegation-progress strip ("Claude is working — <last event>", from the already-broadcast turn events) rides with Worker 2's in-flight chat-bar refactor. The parked screenshot-per-voice-turn idea is now unblocked and re-banked.

**Deferred:** vault meta relocation (wiki-mcp/wiki-service were in flight all round). The backlog keeps that plus the three rows other agents banked mid-blitz (wiki rename/move, oui-embed protocol-relative origin check, createDoc polish).

## 2026-07-12 21:24 — Toolbar chat bar becomes a recording pill with a latest-action ticker (Worker 2)

Per Steve's feedback that the collapsed bar undersold the live mic: the voice cluster is now a *recording pill* — red wash for the whole life of the session, a five-bar equalizer that dances with the live input level while listening (staggered pulse while the model speaks or works, amber for tool runs), the state spelled out ("Listening…" / "Speaking…" / "Working…"), and a truncated latest-action ticker (last utterance from either side, running tool, or error — lifecycle noise skipped) so the collapsed bar is never a black box. Warnings/errors still tint the mic and ride the tooltips; the delegation tooltip and expander busy-dot survive. Browser-verified with live speech: pill red + bars dancing + "Listening…" + the model's spoken answer in the ticker.

## 2026-07-12 21:14 — Delegation respawn fixed: full model ids, fast re-tiered to Sonnet 5 (The Optimizer)

Voice row 11 closed: `delegationModel` returns full model ids instead of CLI aliases, so `smart` on the production default (claude-opus-4-8) is a same-model no-op — the spurious every-delegation CLI respawn (and its 11–21K uncached tokens per API call) is gone, and `fast` moves from Haiku (6.6–8.2s of silent thinking per the agent-timing report) to Sonnet 5. Verified by rerun: smart 3.5s total, respawn=no, no client-visible silent gaps — squarely in the 3.3–3.8s no-switch band (was 4–12s). Fast on Sonnet 5 crosses a real model switch at ~8s with one 3.8s silent gap; that gap is row 12's streaming-progress work, not a model problem. Bonus observation: both delegated writes used the wiki create tool correctly (row 13's sandbox-miss didn't recur after the capability-parity commit).

## 2026-07-12 21:14 — Chat collapses into the toolbar; the pane is opt-in via an expander (Worker 2)

Per Steve's direction, chat's resting state is now "neatly hidden in the toolbar": the chat toggle slides open a compact voice cluster beside the title — mic (pulsing red when live), input-level bar, and a chevron expander — no pane, no text input. The conversation pane (bubbles, D6 chips, the typed composer with screenshot/send, the delegation-progress and voice-problem strips) appears only via the expander, dropping down below the toolbar (`app/chat-expanded` in the D3 store); the toolbar's old `mr-96` slide-clear hack is gone since nothing overlaps it anymore. Voice auto-start/stop still keys off `app/chat-open`. With the pane collapsed, the mic button doubles as the problem indicator (amber tint for warnings, red for failures, message in the tooltip) and the expander carries the reply-in-progress dot. New `chat-bar.tsx`; ChatSidebar is now the pane behind the expander. Browser-verified: open → cluster + auto-start, expand → pane below toolbar, close → mic released (log-confirmed start/end pairs).

## 2026-07-12 21:21 — Embeds become launchable previews; agent-driven create→navigate→write→embed flow proven (main session)

`<oui-embed>` reworked from a break-out card to a **preview inside the document margins**: the artifact renders live at a 1024px virtual width and scales down uniformly to the column (aspect kept — measured matrix(0.686, 0, 0, 0.686) in the reading layout), height snug to content with a 360px cap, under a gradient/desaturate shader that blocks all interaction — "ready to launch, not for poking". One action: **Expand** (button or clicking the shader), which writes the new `app/expanded-artifact` store key. App mounts a full-panel view over the content area — title bar with the artifact name and a **Source** button that minimizes back; the document view underneath stays mounted and open the whole time (verified: scrollTop 300 unchanged through expand→minimize). The store key is documented in both agents' set-state tool descriptions, so either agent can launch or minimize an artifact. Also removed the "New Artifact" / "Edit Artifact" floating test buttons (per Steve).

Proved the full agent flow end-to-end with the front-end agent's own tools: `create_doc` launch-demo.md → `set_app_state` navigate (applyServerUpdates path) → `edit_doc` fills the prose → `create_doc` launch-demo.oui + `edit_doc` inserts the embed at the top of the page. All four calls through `executeVoiceTool`, page hot-reloaded live, both demo files kept in the wiki as the walkthrough.

## 2026-07-12 21:06 — Document-capability parity: create + read-any-file on both agent surfaces (main session)

Audited what the user can do with documents versus the two agents' tools. Two gaps closed, both through the shared wiki-service layer: (1) **file creation** — the user could create .oui via artifact save while the voice agent had no create at all and the Claude session could only make markdown; new `createDoc` (create-only, refuses overwrite, extension allowlist .md/.oui/.html/.txt/.csv/.json, mkdir -p) is now exposed as voice `create_doc` and wiki-MCP `create_file` (allowlisted in the CLI spawn, documented in prompts/wiki.md). (2) **reading non-markdown** — the user views .oui/.html in the panel, but the Claude session's vault tools only see markdown; new wiki-MCP `read_file` serves chunked reads of any wiki file through the same `readDoc` the voice agent already used. Existing parity confirmed for list/read/search/edit-markdown/edit-artifact. Rename/move (in the original Wiki API design, exposed nowhere) banked in the blitz backlog rather than built. Smoke-tested createDoc paths (subfolder create, .oui create, duplicate + bad-extension rejections) and the generated Realtime schema.

## 2026-07-12 21:04 — Prompt-guidance rough edges: deictic screen references, quiet quick actions, incremental artifact builds (The Optimizer)

Three blitz rows cleared as prompt-doc edits for the cohesion hour. voice-agent.md: deictic words ("this/that/here") now explicitly resolve against the screen via get_app_state — never against the conversation topic; quick local actions (navigate, point, small edits) are done silently and confirmed in a word, announcements reserved for slow delegated work. system-prompt.md: new "Build new artifacts incrementally" section — small first ui call (root + style + skeleton + default kebab-case name), then successive edit patches so the user watches the artifact take shape instead of one big reveal. The parked attach-a-screenshot-per-voice-turn idea moved onto the payload-size nit.

## 2026-07-12 21:02 — Wiki preload recurses into subdirectories; AIs get their wiki knowledge back (The Optimizer)

The D7 preload now walks the whole wiki tree (hidden dirs skipped, still whole-files-only smallest-first under the 24KB budget, pages labeled by wiki-relative path), so the docs reorg no longer empties it — design/journeys.md and friends are inline again and grounded asks return to the ~4.4s path. Four unit tests pin the contracts (tests/intelligence/prompt-preload.test.ts); full suite green. Found en route: the vault regenerates docs/meta/ in place, so the design/api relocation went stale immediately — stale copy removed, docs/meta/ re-gitignored, vault-config nit banked.

## 2026-07-12 19:58 — Docs directory organized into subdirectories (The Optimizer)

`docs/` now has `design/` (proposal, ARCHITECTURE, decisions, journeys, explaining-with-clarity, plus the vault-generated `meta/` relocated as `design/api/`), `reports/` (the two/five/six-hour reviews + six-hour .oui, llm-invocation-report), `worklogs/` (the dated logs), and `examples/pr-502764-review/` (the archetype .html + benchmark .oui); README, oui-embed-demo, and wiki-deck stay at root. All links updated (wiki home page web paths, TASKS.md, AGENTS.md incl. the worklog-location convention, tests/client READMEs, skill pointer, protocol doc-comment examples). Known consequence banked in the blitz backlog: D7's wiki preload inlines root-level pages only, so the moved design docs fall out of the preload until it learns subdirectories.

## 2026-07-12 00:14 — Agents can point: `indicate` tool scrolls to and blinks on-screen content (main session)

Both agents can now physically point at content while talking about it. A shared client capability (lib/indicate.ts) resolves a target, scrolls the main panel to it if it isn't visible (smooth, window stays pinned), and pulses an outline+wash blink (3×, ~1.5s). Three target kinds: markdown source lines (via the existing `data-source-line` stamps, with nearest-block fallback for mid-paragraph lines), OpenUI statement names (new `data-statement` stamps on every component root — no schema change, so no ui-library.ts sync needed), and a verbatim text snippet. Exposure: the voice model gets a dedicated `indicate` front-end tool; the Claude session drives the same code by writing the `app/indicate` store key through set_state (documented in the tool description) — the D3 store as API, no new protocol. Verified live: all three target kinds, fallback, and error listings (a missing statement name returns the on-screen names). Also committed alongside: the earlier screenshot-capture switch from PNG to JPEG q0.75.

## 2026-07-12 00:07 — Break (The Optimizer)

Work stopped for the day at 00:07; the evening block (~22:30–00:07) closed strong: the realtime voice agent works end to end from a real microphone (root cause was Chrome's stale audio service — the 23:35 setback note is superseded), D7 shipped wiki preload + the Opus 4.8 default (grounded ask→UI 7.7s→4.4s), the blitz backlog is empty, and both agents carry the Explorer identity. Resume step: exercise the still-unproven voice paths live (wiki edit, screenshot, `ask_artifact_agent` delegation with the phone-style narration), then the declared Phase-4 focus; the voice latency/cost eval (voice row 9, The Optimizer) is ready to run alongside. Time accounting for the block is in [six-hour-review.md](../reports/six-hour-review.md) (chargeable ≈6h20 of 8h, ~1h40 remaining).

## 2026-07-12 late — Agent-as-a-tool timing suite: the 70s file creation decomposed (Cleaner)

New suite `eval/agent-timing.ts` drives `ask_artifact_agent` over `voice:tool` exactly as the browser does (isolated server, throwaway wiki copy) and reconstructs the backend waterfall from the event log. Findings (eval/agent-timing-report.md): every delegation respawns the CLI — `smart`'s "opus" alias never string-matches `claude-opus-4-8`, so even same-model delegations restart; each respawned turn pays 11–21K uncached tokens per API call (model-scoped cache + resume fork); Haiku `fast` adds 6.6–8.2s silent thinking and picked the wrong tool (sandbox `Write` instead of wiki tools — file lands outside the wiki while reporting success) in 2/4 runs. No-switch control: 3.3–3.8s, correct 2/2, no silent gaps. Streamability: client-visible silence 3.1–9.6s per run; cover signals (tool-call block starts, thinking starts) exist within ~2s but chat:tool broadcasts only at message completion and the voice model gets nothing until the blocking tool returns. Fixes banked as voice rows 11–13.

## 2026-07-12 late — Voice rows 11–13 done: no-switch delegation, streamed tool starts, wiki-placement preamble (Cleaner)

Goal: minimal delegated-turn time + live status before the tool call returns. Shipped: both ask_artifact_agent modes default to the session's own `claude-opus-4-8` (a Sonnet-5 `fast` tier measured worse — the respawn dominates, 6.5–8.3s, and exposed a race where the respawned CLI registers zero MCP tools, banked as voice row 14); server broadcasts `chat:tool {phase:"start"}` at tool block start + `chat:status "reasoning"` at thinking starts, chat pane shows/upgrades rows in place; DELEGATION_PREAMBLE teaches wiki tools + verify-before-success. Verified with the agent-timing suite (`agent-timing-final`): 3.6–5.8s (was 4–12s), 4/4 files in the wiki (was 2/4), zero respawns, zero silent gaps > 3s (was up to 9.6s), first tool visible ~1.3s after send.

## 2026-07-12 late — Front-end-agent scenario suite: full wiki journey passes 8/8 (Cleaner)

New `eval/frontend-scenarios.ts` drives the voice-tool registry over the ws exactly as the browser would (no typed chat): orient → create directory+markdown file (delegated; registry has no create tool) → fill content (edit_doc) → generate a .oui artifact in the new directory (delegated; read → create_file → list_files verify) → patch the artifact (edit_artifact, clean by-name merge verified on disk) → edit content again → read everything back. **All 8 steps passed on the first run, zero backend errors**; direct edits are sub-100ms, delegated create 6.5s, delegated artifact generation 18.6s (read round trip + 7s spec streaming + verification). No hard blockers; four P2 rough edges banked as voice rows 15–18 (no direct create tool, dead-end teaching error, folder-creation concept, verify-round-trip cost).
