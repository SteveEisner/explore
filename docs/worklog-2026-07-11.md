# Worklog — 2026-07-11

Dated log of milestones. Newest entries at the top. See AGENTS.md for conventions.

## 2026-07-11 23:14 — Explorer identity taught to both agents (The Optimizer)

Per Steve's direction, both prompt docs now teach the app's purpose — a vault of documents/data whose author is the learner; discuss to capture new raw input into the wiki, co-explore the files, build custom explanation UIs — and the single-virtual-agent rule from both sides. `voice-agent.md`: phone-style progress narration during delegation, "as few words as possible" casual tone, new "Growing the wiki" capture section. `system-prompt.md`: matching Explorer role, treat voice-relayed requests as the same user with no mention of another agent, spoken-friendly lead sentences.

## 2026-07-11 23:10 — Realtime voice agent implemented, rows 1–8 (Worker 2)

The D5 voice collaborator is built end to end: `POST /api/voice/session` mints an ephemeral client secret with the full session fixed server-side (verified against the current Realtime docs — `gpt-realtime-2.1`, `/v1/realtime/client_secrets`, secret in `value`; an accidental test run against Steve's real key returned 200, proving the shape live); a mic toggle in the chat sidebar opens the WebRTC conversation (semantic VAD, barge-in, visible listening/speaking/tool states, 2-min idle auto-close). One zod registry (`server/src/voice-tools.ts`) drives both the schemas OpenAI sees and the server executor behind the new `voice:tool` ws family; front-end tools (get/set app state, screenshot-as-image-input) run in the browser off the data channel. The new wiki service (`wiki-service.ts`) implements chunked reads, grep-style in-process search (chose that over driving the untested vault MCP), and D1 str_replace edits with loud no-match/ambiguous errors. `ask_artifact_agent` delegates to the Claude session with fast→haiku / smart→opus via restart-with-`--resume` model switching (only ever when idle — a turn-resolver FIFO serializes against typed chat; `set_model` control requests exist only in reverse-engineered protocol docs, so noted as a later optimization). Transcripts fold into every client's chat as `via: "voice"` messages carrying the D6 `{text, stateSnapshot}` envelope into the event log. 24 new tests (wiki service unit + real-server bridge suite; harness gained per-test env overrides to mask `.env.local`). Remaining: row 9 (Optimizer's eval) and row 10 — a human speaking into a real mic. Note: `App.tsx`'s 3-line voice wiring is uncommitted — it is interleaved with the sound-cues agent's in-flight work and rides with their commit.

## 2026-07-11 23:05 — Blitz backlog cleared, 5 of 6 items (the Cleaner)

Ran the banked blitz as four background agents plus two inline fixes; each landed as its own commit through the typecheck+lint gate:

- **docs/meta/ untracked + gitignored** — vault-generated (`generated_by: mcp-markdown-vault`); nothing pins it for the clean-clone demo, files stay on disk.
- **JSONL logger typed** — `log` is now generic over a structural `LoggableEvent { type: string }`; the two `as unknown as Record<string, unknown>` casts in chat.ts are gone.
- **client/README.md rewritten** — Vite boilerplate replaced with an accurate workspace README (real scripts, pointer to root README/ARCHITECTURE).
- **Inline backticks render as code chips** — root cause was CSS, not the markdown pipeline: Tailwind typography redraws literal backticks as `::before/::after` and gives inline code no chip. Unlayered `.prose` override in index.css (unlayered so it beats the plugin's `@layer utilities`).
- **Horizontal window pan fixed** — `overflow: clip` (with `hidden` fallback) on html/body, the shell div, and the chat aside; `hidden` still leaves a programmatically scrollable box, `clip` removes the scroll container so focus/scrollIntoView can't pan the window.

**Deferred:** TypeScript version alignment (client `~6.0.2` vs server `^5.8.0`) — needs `npm install` and `client/package.json`/lockfile edits, which were carrying another agent's in-flight typeset/oui-embed work; stays banked with a note. Note for that work: my backtick chip override targets `.prose` (chat) only — if the wiki-document surface finishes migrating to `.typeset`, check the two treatments stay consistent.

## 2026-07-11 23:03 — Wiki reading layout (shadcn/typeset) + `<oui-embed>` markdown tag (main session)

Markdown file viewing got a real reading layout: centered 3xl column with generous margins, styled by **shadcn/typeset** (released this month — one CSS file, `typeset.css`, imported after Tailwind; the file-viewer surface uses `typeset typeset-docs`, chat keeps compact `prose`). Code blocks keep a dark surface in both themes since highlight.js uses the github-dark scale. Mermaid rendering was already in place from earlier work — verified, no changes needed beyond a `not-typeset` escape on the diagram wrapper.

New capability: wiki markdown can now embed a live OpenUI application with `<oui-embed src="file.oui"></oui-embed>` (rehype-raw + sanitize-schema allowlist + a component mapping to `GenerativeView`). Embeds hot-reload off `app/wiki-changed` like the file viewer. Two gotchas found in browser verification: (1) a one-line custom tag is *inline* HTML to micromark, so it lands inside a `<p>` — the renderer unwraps such paragraphs; (2) artifacts use fixed nav/aside pixel widths tuned for the full panel and collapse to one-word lines in a 48rem column — embeds now break out of the reading measure up to 72rem with horizontal scroll as fallback. Verified end-to-end in Chrome against pr-502764-review.oui (tabs interactive inside the embed).

Lesson: dev-server screenshots from a backgrounded tab go stale and rAF never fires — verify paint with `elementFromPoint`, not eyeballs.

## 2026-07-11 22:52 — Feedback channels prioritized; D6 unified feedback envelope (The Optimizer)

Channel review with Steve produced three decisions: point-and-comment reframed as statement-name-addressed (P1, Multimodal section), interaction signals via a per-turn D3 store digest (P2, Intelligence section), and D6 — one feedback envelope `{text?, screenshot?, statementRef?, stateSnapshot}` for every channel, with stateSnapshot making feedback self-locating. Voice transcript integration (row 8, Worker 2) is directed to emit the envelope rather than a voice-specific format.

## 2026-07-11 22:36 — Voice agent designed and broken into tasks (The Optimizer)

Realtime voice editing specced with Steve and recorded as decisions.md D5: OpenAI gpt-realtime as a second intelligence, browser-direct WebRTC audio with server-minted ephemeral tokens, tools bridged over the existing `/ws` (`voice:tool`), direct wiki/artifact/state editing plus an `ask_artifact_agent` delegation tool, toggle-mic open conversation. Eight task rows created for assignment in the new "Voice agent (realtime editing)" section of TASKS.md; the dormant Wiki API read/edit rows move onto the critical path via a shared wiki service layer. Next: assign rows 1–3 (critical path).

## 2026-07-12 — Knob follow-up sweep: wiki preload proven (Cleaner, for The Optimizer)

Five next-knob candidates from the timing sweep, tested or resolved by analysis (report section added to eval/sweep-report.md): **wiki preload proven** — inlining the wiki page into the appended system prompt with a "use this copy" instruction cuts grounded ask→UI 7.7s→4.4s (Opus 4.8) / 5.2s→4.4s (Sonnet 5), drops the read turn (3→2), stays byte-exact, and is slightly cheaper; fixed scenario unregressed. Harness now accepts arbitrary prompt variants (`eval/prompts/<name>.md`). Fast mode blocked (no headless CLI flag); progressive spec rendering worth 0.8–1.4s (client work, `uiFirstDeltaMs` already measured); prompt cache verified healthy (59/59 turns, ~2 uncached tokens); post-UI tail is structural, not verbosity.

Ops notes: (1) 3 sweep runs died when concurrent server edits (voice work) briefly broke startup — re-run cells in `sweep-preload-topup`; consider a pinned side instance for future sweeps. (2) These changes were staged when commit 94d8ac9 (wiki reading layout) ran and rode along in it — content verified intact in HEAD; shared-index hazard to watch when two agents commit near-simultaneously.

## 2026-07-12 — D7 shipped: wiki preload in production; Opus 4.8 default (Cleaner)

Sweep results recorded as decisions.md D7 and implemented: `buildSystemPrompt` now inlines small root-level wiki pages into the appended prompt (smallest-first, whole files only, 24KB budget, `WIKI_PRELOAD_BYTES` overrides / 0 disables) with a use-this-copy + re-read-after-edit instruction; the server's default model is pinned to `claude-opus-4-8` (`CLAUDE_MODEL` still overrides). Verified end-to-end with the eval on the production prompt path (`eval/results/verify-preload-prod/`): grounded ask→UI 7.7s→4.6s median, read turn eliminated (3→2), 6/6 byte-exact, fixed scenario unregressed. Eval README documents the new knob and the generalized `--prompt <name>` variants.
