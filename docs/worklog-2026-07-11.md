
## 2026-07-13 — Agent-as-a-tool timing suite: the 70s file creation decomposed (Cleaner)

New suite `eval/agent-timing.ts` drives `ask_artifact_agent` over `voice:tool` exactly as the browser does (isolated server, throwaway wiki copy) and reconstructs the backend waterfall from the event log. Findings (eval/agent-timing-report.md): every delegation respawns the CLI — `smart`'s "opus" alias never string-matches `claude-opus-4-8`, so even same-model delegations restart; each respawned turn pays 11–21K uncached tokens per API call (model-scoped cache + resume fork); Haiku `fast` adds 6.6–8.2s silent thinking and picked the wrong tool (sandbox `Write` instead of wiki tools — file lands outside the wiki while reporting success) in 2/4 runs. No-switch control: 3.3–3.8s, correct 2/2, no silent gaps. Streamability: client-visible silence 3.1–9.6s per run; cover signals (tool-call block starts, thinking starts) exist within ~2s but chat:tool broadcasts only at message completion and the voice model gets nothing until the blocking tool returns. Fixes banked as voice rows 11–13.
